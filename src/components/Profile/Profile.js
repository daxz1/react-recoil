import React from "react";

/**
 *
 * @param username
 * @returns {*}
 */
function Profile({ username }) {
  return (
    <div>
      Profile for { username }
    </div>
  )
};

export default Profile;